# Code generated by fabricator-generate-project-go
#
# Modifications in code regions will be lost during regeneration!
# region CODE_REGION(CI)
name: generic_lint_pr
on:
  pull_request:
    branches: [main]

env:
  # Golang version to use across CI steps
  GOLANG_VERSION: "1.17.6"

jobs:
  build-go:
    name: Build
    uses: roueslibres1/gh-actions/.github/workflows/generic-pr-check.yml@main
    with:
      actions-ref: main
    secrets:
      token: ${{ secrets.CI_TOKEN }}
# endregion
